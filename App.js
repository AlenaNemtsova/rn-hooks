import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import TodoList from './scr/components/TodoList';

export default function App() {
  return (
    <View>
      <TodoList />
    </View>
  );
};

const styles = StyleSheet.create({});